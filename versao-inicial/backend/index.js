const app = require('express')()                  //importando o express
app.listen(3000, () => {                         //porta que iremos usar
    console.log('Backend executable...')         //msg que aparecera no terminal ao rodarmos o npm start
})